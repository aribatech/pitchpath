version: "3.9"

services:
  mongodb:
    image: mongo:7
    container_name: internship_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  redis:
    image: redis:7-alpine
    container_name: internship_redis
    ports:
      - "6379:6379"

volumes:
  mongo_data:
